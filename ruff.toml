# Python quality findings are informational while the existing backlog is reviewed.
target-version = "py310"
cache-dir = ".cache/ruff"
extend-exclude = [".esphome", "build", "build-*", "managed_components", "third_party", "vendor"]

[lint]
select = ["E4", "E7", "E9", "F", "B"]
